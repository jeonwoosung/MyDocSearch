server:
  port: 8080

spring:
  datasource:
    url: "jdbc:h2:file:${APP_DB_PATH:./db/mailsearch};AUTO_SERVER=TRUE"
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

app:
  eml-root: "/data/documents"
  index-dir: "./data/lucene-index"
  db-path: "./db/mailsearch"
  auth-username: "admin"
  auth-password: "admin"
